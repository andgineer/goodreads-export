{{ clean_file_name(author) }} - {{ clean_file_name(series.title) }} - series.md

{%
        set search_params = urlencode(
            {
                "utf8": "✓",
                "q": series.title + ", #",
                "search_type": "books",
                "search[field]": "title",
            }
        )
-%}
[[{{ clean_file_name(author) }}]]
[{{ series }}](https://www.goodreads.com/search?{{ search_params }})
